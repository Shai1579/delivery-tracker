export function Msg({ msg }) {
    return (
        <main className="msg flex center">
                <div className="txt">
                    <h3>{msg}</h3>
                </div>
        </main>
    )
}